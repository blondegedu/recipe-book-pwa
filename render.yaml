services:
  - type: web
    name: recipe-book
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python recipe_server_multiuser.py
    envVars:
      - key: PORT
        value: 10000
      - key: HOST
        value: 0.0.0.0
